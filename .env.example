# AI Web Engine Server Configuration
# Copy this file to .env and configure for your environment
# Most defaults point to softagen.com - change SERVER_HOST to use a different server

# Server host (where the AI Web Engine APIs are hosted)
SERVER_HOST=https://softagen.com

# API endpoints (derived from SERVER_HOST)
API_TYPES_BASE_URL=${SERVER_HOST}/api/types/v0.1.0
OPENAPI_URL=${SERVER_HOST}/engine/openapi.json

# OAuth Configuration
OAUTH_ISSUER=${SERVER_HOST}
OAUTH_CLIENT_ID=
OAUTH_SCOPE=openid
OAUTH_REDIRECT_HOST=127.0.0.1
OAUTH_REDIRECT_PORT=0

# GraphQL Configuration
SCHEMA_ENDPOINT=${SERVER_HOST}/graphql
GRAPHQL_METHOD=POST
# GRAPHQL_COOKIE=
# GRAPHQL_REFERER=
# CSRF_TOKEN=

# Authentication Tokens (generated by oauth-login script)
# OAUTH_TOKEN=
# BEARER_TOKEN=
